// handle report user
const handleReportUser = (uid) => {
    // report user of uid
    console.log("reported user", uid);
}

// handle flag user
const handleFlagUser = (uid) => {
    // flag user of uid
    console.log("flag user", uid);
}

export { handleReportUser, handleFlagUser };